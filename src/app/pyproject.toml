[project]
name = "afc"
version = "0.0.1"
description = "Audio file converter"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "filetype>=1.2.0",
    "pydub>=0.25.1",
]

[dependency-groups]
dev = [
    "pytest>=8.4.0",
]
